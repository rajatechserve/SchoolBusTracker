// Legacy AuthStack replaced by expo-router routes (login.tsx, driver-login.tsx, parent-login.tsx).
// Keeping file as a no-op export to avoid stale imports; safe to delete after confirming removal.
export default function AuthStack(){ return null; }
